{
  "name": "node-ts-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node14.19.0 --outdir=build",
    "start": "npm run build && node build/index.js",
    "dev": "nodemon"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "rimraf": "^5.0.1",
    "@types/node": "^20.5.1",
    "esbuild": "^0.19.2",
    "nodemon": "^3.0.1",
    "typescript": "^5.1.6"
  }
}
